<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/action_save_list"
        android:icon="@android:drawable/ic_menu_save"
        android:title="@string/action_save_list"
        app:showAsAction="ifRoom"/>
    <item
        android:id="@+id/action_disable_warnings"
        android:title="@string/action_dismiss_warnings"
        android:checkable="true"
        app:showAsAction="never" />
    <item
        android:id="@+id/action_disable_errors"
        android:title="@string/action_disable_errors"
        android:checkable="true"
        app:showAsAction="never" />
</menu>